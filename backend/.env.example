# Configuration de la base de données MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=votre_mot_de_passe
DB_NAME=gargaa_transport
DB_PORT=3306

# Configuration du serveur
PORT=5000

# Clé secrète JWT (générez une clé aléatoire forte)
JWT_SECRET=votre_cle_secrete_jwt_tres_longue_et_aleatoire

# Durée de validité du token (en heures)
JWT_EXPIRES_IN=24h

